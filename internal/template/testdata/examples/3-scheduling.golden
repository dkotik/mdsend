subject: scheduling deliveries
seed_key: TGubmnumbB3
schedule_after: 2026-12-30T00:30:00Z


Schedule Messages for Later Delivery
====================================

Queued messages can be delayed by providing `schedule` frontmatter instructions:


- **after:** time until the message can be sent.**after:** time until the message can be sent.
- **delay:** time duration added to `after` or current time.**delay:** time duration added to `after` or current time.
- **step:** time duration added for each recipient.**step:** time duration added for each recipient.
- **fluctuate:** normalized random time duration added for each mailing step, including the first message.**fluctuate:** normalized random time duration added for each mailing step, including the first message.
- **expire:** time duration, after all the messages have been sent, to keep the letter and message records in the database before they are deleted.**expire:** time duration, after all the messages have been sent, to keep the letter and message records in the database before they are deleted.


Context
-------

Template execution context contains the following fields:


- **Frontmatter:** the raw Markdown frontmatter as `map[string]any`.**Frontmatter:** the raw Markdown frontmatter as `map[string]any`.
- **Recipient:** the raw message target as `map[string]any`. It
will contain `name` and `email` fields and any other fields
fromt the contact source.**Recipient:** the raw message target as `map[string]any`. Itwill contain `name` and `email` fields and any other fieldsfromt the contact source.
- **Content:** rendered Markdown body.**Content:** rendered Markdown body.
- **Schedule:****Schedule:**


Reify Templates
---------------

Special `reify` function stores the output of a



===========[HTML]=============

<!doctype html>
<html lang="en">

<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
  <title>scheduling deliveries</title>
</head>

<body style="color: #1d1d1d;">
  <div style="background-color: #e8ecfb; padding: 0.5em 1em 0.5em 1.5em;">
    <h1>Schedule Messages for Later Delivery</h1>
<p>Queued messages can be delayed by providing <code>schedule</code> frontmatter instructions:</p>
<ul>
<li><strong>after:</strong> time until the message can be sent.</li>
<li><strong>delay:</strong> time duration added to <code>after</code> or current time.</li>
<li><strong>step:</strong> time duration added for each recipient.</li>
<li><strong>fluctuate:</strong> normalized random time duration added for each mailing step, including the first message.</li>
<li><strong>expire:</strong> time duration, after all the messages have been sent, to keep the letter and message records in the database before they are deleted.</li>
</ul>
<h2>Context</h2>
<p>Template execution context contains the following fields:</p>
<ul>
<li><strong>Frontmatter:</strong> the raw Markdown frontmatter as <code>map[string]any</code>.</li>
<li><strong>Recipient:</strong> the raw message target as <code>map[string]any</code>. It
will contain <code>name</code> and <code>email</code> fields and any other fields
fromt the contact source.</li>
<li><strong>Content:</strong> rendered Markdown body.</li>
<li><strong>Schedule:</strong></li>
</ul>
<h2>Reify Templates</h2>
<p>Special <code>reify</code> function stores the output of a</p>

  </div>
</body>

</html>
